## ADDED Requirements

### Requirement: NestJS Server Setup
The system SHALL have a NestJS backend initialized in the `server` directory using TypeScript.

#### Scenario: Verify NestJS Initialization
- **WHEN** the command `npm run start:dev` is executed in the `server` directory
- **THEN** the NestJS server starts successfully on port 3000
